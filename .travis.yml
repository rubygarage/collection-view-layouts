language: swift
osx_image: xcode11.1

cache: cocoapods
podfile: Example/Podfile

before_install:
- bundle install
- bundle exec pod install --project-directory=Example

script:
- set -o pipefail && xcodebuild -workspace Example/collection-view-layouts.xcworkspace -scheme collection-view-layouts-Example -configuration 'Debug' -sdk 'iphonesimulator13.1' -destination 'platform=iOS Simulator,OS=13.1,name=iPhone 8' test | xcpretty -f `xcpretty-travis-formatter`

after_success:
  - bash <(curl -s https://codecov.io/bash)
